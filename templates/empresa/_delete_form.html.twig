
<form class="col" method="post" action="{{ path('empresa_delete', {'id': empresa.id}) }}" onsubmit="return confirm('¿Estas seguro que quieres eliminar esta empresa?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ empresa.id) }}">
    <button class="btn btn-danger btn-sm">Eliminar</button>
</form>
